version: 2.1
jobs:
  build:
    working_directory: ~/automated-tests
    docker:
      - image: driebit/node-gulp
    steps:
      - checkout
      - run:
          name: Update npm
          command: 'npm install -g npm@latest'
      - run:
          name: Install gulp
          command: 'npm i -g gulp'
      - run:
          name: Install dependencies
          command: 'npm i'
      - save_cache:
          key: dependency-cache-{{ checksum "package.json" }}
          paths:
            - node_modules

  test:
    docker:
      - image: driebit/node-gulp
    steps:
      - run:
          name: Test
          command: 'gulp'
      - store_artifacts:
          path: ~/automated-tests/tmp/report.html

workflows:
  version: 2
  build_and_test:
    jobs:
      - build:
          filters:
            branches:
              only: master
      - test:
          requires:
            - build